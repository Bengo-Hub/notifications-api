{{ or .company_name "ISP" }}: Hi {{ .customer_name }}, your {{ .package_name }} ({{ .package_type }}) is active. Username: {{ .username }} Password: {{ .password }}{{ if .expiry_at }} Valid until {{ .expiry_at }}{{ end }}.
