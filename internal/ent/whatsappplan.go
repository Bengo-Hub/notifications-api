// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/notifications-api/internal/ent/whatsappplan"
	"github.com/google/uuid"
)

// WhatsAppPlan is the model entity for the WhatsAppPlan schema.
type WhatsAppPlan struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Plan display name (e.g. Basic, Standard, Pro)
	Name string `json:"name,omitempty"`
	// URL-safe unique identifier (e.g. basic, standard, pro)
	Slug string `json:"slug,omitempty"`
	// Monthly subscription price in KES charged to tenant
	PriceMonthly float64 `json:"price_monthly,omitempty"`
	// Estimated monthly provider cost — platform profit = price_monthly - provider_cost
	ProviderCost float64 `json:"provider_cost,omitempty"`
	// Message quota per month; 0 = unlimited
	MessagesPerMonth int `json:"messages_per_month,omitempty"`
	// Whether this plan is available for new subscriptions
	IsActive bool `json:"is_active,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WhatsAppPlanQuery when eager-loading is set.
	Edges        WhatsAppPlanEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WhatsAppPlanEdges holds the relations/edges for other nodes in the graph.
type WhatsAppPlanEdges struct {
	// Subscriptions holds the value of the subscriptions edge.
	Subscriptions []*TenantWhatsAppSubscription `json:"subscriptions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SubscriptionsOrErr returns the Subscriptions value or an error if the edge
// was not loaded in eager-loading.
func (e WhatsAppPlanEdges) SubscriptionsOrErr() ([]*TenantWhatsAppSubscription, error) {
	if e.loadedTypes[0] {
		return e.Subscriptions, nil
	}
	return nil, &NotLoadedError{edge: "subscriptions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WhatsAppPlan) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case whatsappplan.FieldIsActive:
			values[i] = new(sql.NullBool)
		case whatsappplan.FieldPriceMonthly, whatsappplan.FieldProviderCost:
			values[i] = new(sql.NullFloat64)
		case whatsappplan.FieldMessagesPerMonth:
			values[i] = new(sql.NullInt64)
		case whatsappplan.FieldName, whatsappplan.FieldSlug:
			values[i] = new(sql.NullString)
		case whatsappplan.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case whatsappplan.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WhatsAppPlan fields.
func (_m *WhatsAppPlan) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case whatsappplan.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case whatsappplan.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case whatsappplan.FieldSlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field slug", values[i])
			} else if value.Valid {
				_m.Slug = value.String
			}
		case whatsappplan.FieldPriceMonthly:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price_monthly", values[i])
			} else if value.Valid {
				_m.PriceMonthly = value.Float64
			}
		case whatsappplan.FieldProviderCost:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field provider_cost", values[i])
			} else if value.Valid {
				_m.ProviderCost = value.Float64
			}
		case whatsappplan.FieldMessagesPerMonth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field messages_per_month", values[i])
			} else if value.Valid {
				_m.MessagesPerMonth = int(value.Int64)
			}
		case whatsappplan.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		case whatsappplan.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WhatsAppPlan.
// This includes values selected through modifiers, order, etc.
func (_m *WhatsAppPlan) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySubscriptions queries the "subscriptions" edge of the WhatsAppPlan entity.
func (_m *WhatsAppPlan) QuerySubscriptions() *TenantWhatsAppSubscriptionQuery {
	return NewWhatsAppPlanClient(_m.config).QuerySubscriptions(_m)
}

// Update returns a builder for updating this WhatsAppPlan.
// Note that you need to call WhatsAppPlan.Unwrap() before calling this method if this WhatsAppPlan
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *WhatsAppPlan) Update() *WhatsAppPlanUpdateOne {
	return NewWhatsAppPlanClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the WhatsAppPlan entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *WhatsAppPlan) Unwrap() *WhatsAppPlan {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: WhatsAppPlan is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *WhatsAppPlan) String() string {
	var builder strings.Builder
	builder.WriteString("WhatsAppPlan(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("slug=")
	builder.WriteString(_m.Slug)
	builder.WriteString(", ")
	builder.WriteString("price_monthly=")
	builder.WriteString(fmt.Sprintf("%v", _m.PriceMonthly))
	builder.WriteString(", ")
	builder.WriteString("provider_cost=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProviderCost))
	builder.WriteString(", ")
	builder.WriteString("messages_per_month=")
	builder.WriteString(fmt.Sprintf("%v", _m.MessagesPerMonth))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// WhatsAppPlans is a parsable slice of WhatsAppPlan.
type WhatsAppPlans []*WhatsAppPlan
